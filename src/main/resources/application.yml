spring:
    datasource:
      url: jdbc:mysql://127.0.0.1/devices_services
      username: root
      password: password
      driver-class-name: com.mysql.jdbc.Driver
    liquibase:
      change-log: classpath:db/liquibase-changelog.yaml
      enabled: true

